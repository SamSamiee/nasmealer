import React from "react";
import HamMenu from "../HamMenu";

function Navbar({}) {
  return (
    <div>
      <nav>
        <div>logo</div>
        <HamMenu>==</HamMenu>
      </nav>
    </div>
  );
}

export default Navbar;
